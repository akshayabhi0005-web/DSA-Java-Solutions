# Rotate Array

## Problem

Given an integer array `nums`, rotate the array to the right by `k` steps.

`k` is a non-negative integer.

## Examples

### Example 1

**Input:**

```text
nums = [1,2,3,4,5,6,7]
k = 3
