# Majority Element II

## Problem

Given an integer array `nums` of size `n`, find all elements that appear more than `n/3` times.

Return the answer as a list.

It is possible to have at most **two** elements that appear more than `n/3` times.

## Examples

### Example 1

**Input:**

```text
nums = [3,2,3]
