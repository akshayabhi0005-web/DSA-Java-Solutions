# Move Zeroes

## Problem

Given an integer array `nums`, move all `0`s to the end of the array while keeping the relative order of the non-zero elements.

The solution must modify the array **in-place**, without creating another array.

## Example

### Input
```text
[0, 1, 0, 3, 12]
